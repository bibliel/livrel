{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "section",
    "title": "Section",
    "description": "",
    "type":"object",
    "properties": {
        "type":{
            "description": "Type of the document",
            "type": "string",
            "enum": ["dialog"]
        }
    },
    "required": ["type"]
}