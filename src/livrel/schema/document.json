{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "document",
    "title": "Document",
    "description": "",
    "type":"object",
    "properties": {
        "type":{
            "description": "Type of the document",
            "type": "string",
            "enum": ["dialog"]
        }
    },
    "required": ["type"]
}